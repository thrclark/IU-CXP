import { FormGroup, UntypedFormGroup, UntypedFormArray, FormArray, FormControl, ControlContainer } from '@angular/forms';
import * as i0 from '@angular/core';
import { inject, Input, Directive, NgModule, InjectionToken } from '@angular/core';
import { MessageService } from 'espd-common/i18n';
import { UnsubscribeOnDestroyDirective } from 'espd-common/utils';
import { takeUntil } from 'rxjs/operators';

class EspdFormGroup extends FormGroup {
    constructor() {
        super(...arguments);
        this.serverErrors = undefined;
        this._isSubmitted = false;
    }
    get hasServerSideErrors() {
        return this.serverErrorCount > 0;
    }
    get isSubmitted() {
        return this._isSubmitted || isAncestorSubmitted(this);
    }
    set isSubmitted(isSubmitted) {
        this._isSubmitted = isSubmitted;
    }
    markAsPristine() {
        super.markAsPristine();
        this.isSubmitted = false;
        this.clearServerErrors();
    }
    reset(value = {}, options = {}) {
        this.clearServerErrors(options?.onlySelf);
        super.reset(value, options);
    }
    clearServerErrors(onlySelf = false) {
        this.serverErrors = undefined;
        if (!onlySelf) {
            for (const control in this.controls) {
                clearNestedServerErrors(this.controls[control]);
            }
        }
    }
    get showValidationStyles() {
        return this.showClientValidationMessages;
    }
    get showClientValidationMessages() {
        return this.invalid && (this.isSubmitted || this.touched);
    }
    get errorCount() {
        const nestedErrors = Object.keys(this.controls)
            .map(k => this.get(k))
            .filter(isEspdControl)
            .map(c => c.errorCount)
            .reduce((previous, current) => previous + current, 0);
        const serverErrors = this.serverErrors ? this.serverErrors.length : 0;
        const frontendErrors = this.errors ? Object.keys(this.errors).length : 0;
        return nestedErrors + serverErrors + frontendErrors;
    }
    get serverErrorCount() {
        const nestedErrors = Object.keys(this.controls)
            .map(k => this.get(k))
            .filter(isEspdControl)
            .map(c => c.serverErrorCount)
            .reduce((previous, current) => previous + current, 0);
        const serverErrors = this.serverErrors ? this.serverErrors.length : 0;
        return nestedErrors + serverErrors;
    }
}

var Sizes;
(function (Sizes) {
    Sizes[Sizes["xs"] = 2] = "xs";
    Sizes[Sizes["sm"] = 4] = "sm";
    Sizes[Sizes["md"] = 8] = "md";
    Sizes[Sizes["lg"] = 12] = "lg";
})(Sizes || (Sizes = {}));
const clearNestedServerErrors = (control) => {
    if (isEspdControl(control)) {
        control.clearServerErrors(false);
    }
    else if (control instanceof UntypedFormGroup) {
        for (const c in control.controls) {
            if (Object.prototype.hasOwnProperty.call(control.controls, c)) {
                clearNestedServerErrors(control.controls[c]);
            }
        }
    }
    else if (control instanceof UntypedFormArray) {
        for (const c of control.controls) {
            clearNestedServerErrors(c);
        }
    }
};
const isEspdControl = (control) => !!control && Object.prototype.hasOwnProperty.call(control, 'serverErrors');
const getAllFormValidationErrors = (group, root = '') => {
    if (root) {
        root = `${root}.`;
    }
    let errors = getControlErrors(group, root);
    Object.keys(group.controls).forEach(key => {
        const control = group.controls[key];
        errors = errors.concat(getNestedFormValidationErrors(control, `${root}${key}`));
    });
    return errors;
};
const isAncestorSubmitted = (control) => {
    let ancestor = control.parent;
    let isSubmitted = false;
    while (ancestor && !isSubmitted) {
        if (ancestor instanceof EspdFormGroup) {
            isSubmitted = ancestor.isSubmitted;
        }
        ancestor = ancestor.parent;
    }
    return isSubmitted;
};
const getNestedFormValidationErrors = (control, root) => {
    let errors = getControlErrors(control, root);
    if (control instanceof UntypedFormGroup) {
        Object.keys(control.controls).forEach(key => {
            errors = errors.concat(getNestedFormValidationErrors(control.controls[key], `${root}.${key}`));
        });
    }
    else if (control instanceof UntypedFormArray) {
        control.controls.forEach((c, i) => {
            errors = errors.concat(getNestedFormValidationErrors(c, `${root}[${i}]`));
        });
    }
    return errors;
};
const getControlErrors = (control, root) => {
    const controlErrors = control.errors;
    if (controlErrors !== null) {
        return Object.keys(controlErrors).map(errorKey => ({
            controlName: root,
            errorName: errorKey,
            errorValue: controlErrors[errorKey]
        }));
    }
    else {
        return [];
    }
};

class EspdFormArray extends FormArray {
    constructor() {
        super(...arguments);
        this.serverErrors = undefined;
    }
    reset(value, options) {
        this.clearServerErrors(options?.onlySelf);
        super.reset(value, options);
    }
    get showValidationStyles() {
        return this.showClientValidationMessages || (!!this.serverErrors && this.serverErrors.length > 0);
    }
    get showClientValidationMessages() {
        return this.invalid && (isAncestorSubmitted(this) || this.touched);
    }
    clearServerErrors(onlySelf) {
        this.serverErrors = undefined;
        if (!onlySelf) {
            for (const c of this.controls) {
                clearNestedServerErrors(c);
            }
        }
    }
    get errorCount() {
        const nestedErrors = this.controls
            .filter(isEspdControl)
            .map(c => c.errorCount)
            .reduce((previous, current) => previous + current, 0);
        const serverErrors = this.serverErrors ? this.serverErrors.length : 0;
        const frontendErrors = this.errors ? Object.keys(this.errors).length : 0;
        return nestedErrors + serverErrors + frontendErrors;
    }
    get serverErrorCount() {
        const nestedErrors = this.controls
            .filter(isEspdControl)
            .map(c => c.serverErrorCount)
            .reduce((previous, current) => previous + current, 0);
        const serverErrors = this.serverErrors ? this.serverErrors.length : 0;
        return nestedErrors + serverErrors;
    }
}

// The TValue|null is required for compilation to succeed at the moment, but it unfortunately forces all controls to be nullable even if undesireable.
class EspdFormControl extends FormControl {
    constructor() {
        super(...arguments);
        this.serverErrors = undefined;
    }
    reset(formState, options) {
        this.clearServerErrors();
        super.reset(formState, options);
    }
    get showValidationStyles() {
        return this.showClientValidationMessages || this.serverErrorCount > 0;
    }
    get showClientValidationMessages() {
        return !!this.errors && (isAncestorSubmitted(this) || this.touched || this.dirty);
    }
    clearServerErrors() {
        this.serverErrors = undefined;
    }
    get errorCount() {
        let count = 0;
        if (this.showValidationStyles) {
            if (this.errors) {
                count += Object.keys(this.errors).length;
            }
            count += this.serverErrorCount;
        }
        return count;
    }
    get serverErrorCount() {
        return this.serverErrors ? this.serverErrors.length : 0;
    }
    get invalid() {
        return (!!this.errors || !!this.serverErrors) && this.showValidationStyles;
    }
}

class ControlNameDirective {
    constructor() {
        this.controlContainer = inject(ControlContainer);
    }
    ngOnInit() {
        if (this.espdControlName !== undefined) {
            const parentGroup = this.controlContainer.control;
            if (parentGroup instanceof UntypedFormGroup) {
                this.control = parentGroup.controls[this.espdControlName];
            }
            if (parentGroup instanceof UntypedFormArray && (typeof this.espdControlName === 'number')) {
                this.control = parentGroup.controls[this.espdControlName];
            }
        }
        if (!this.control) {
            throw new Error(`No control found with name ${this.espdControlName ?? 'undefined'}`);
        }
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: ControlNameDirective, deps: [], target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "20.3.15", type: ControlNameDirective, isStandalone: true, selector: "[espdControlName]", inputs: { espdControlName: "espdControlName" }, ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: ControlNameDirective, decorators: [{
            type: Directive,
            args: [{
                    selector: '[espdControlName]'
                }]
        }], propDecorators: { espdControlName: [{
                type: Input
            }] } });

/** @deprecated import ControlNameDirective directly */
class ControlNameModule {
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: ControlNameModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule }); }
    static { this.ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "20.3.15", ngImport: i0, type: ControlNameModule, imports: [ControlNameDirective], exports: [ControlNameDirective] }); }
    static { this.ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: ControlNameModule }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: ControlNameModule, decorators: [{
            type: NgModule,
            args: [{
                    imports: [
                        ControlNameDirective
                    ],
                    exports: [
                        ControlNameDirective
                    ]
                }]
        }] });

const espdAbstractInputComponentInjectionToken = new InjectionToken('EspdAbstractInputComponent');
class EspdAbstractInputDirective extends UnsubscribeOnDestroyDirective {
    static { this.nextId = 0; }
    set placeholder(value) {
        this.ngUnsubscribe.next();
        this.updatePlaceholder(value);
        this.messageService.onDataUpdate.pipe(takeUntil(this.ngUnsubscribe)).subscribe(() => {
            this.updatePlaceholder(value);
        });
    }
    get placeholder() {
        return this.placeholderText;
    }
    // Keep constructor param until we're ready to refactor
    // eslint-disable-next-line @angular-eslint/prefer-inject
    constructor(_injector) {
        super();
        this.labelledby = null;
        this.labelSrOnly = false;
        this.disabled = false;
        this.required = false;
        this.hideErrorMessages = false;
        /** An ID to identify this component instance. Used for generating unique IDs in the DOM */
        this.id = EspdAbstractInputDirective.nextId++;
        this.messageService = inject(MessageService);
        this.placeholderText = '';
        this.controlNameDirective = inject(ControlNameDirective, { host: true, optional: true });
        this.delegates = [];
    }
    ngOnInit() {
        if (this.controlNameDirective) {
            this.control = this.controlNameDirective.control;
        }
        if (!this.errorMessages) {
            this.errorMessages = {};
        }
    }
    ngOnChanges(_changes) {
        this.delegates.forEach(delegate => delegate.onInputConfigurationChange());
    }
    get ariaLabelledby() {
        if (this.labelledby) {
            return this.labelledby;
        }
        else {
            return (this.label ? `espd-input-${this.id}` : '') + (this.directions ? ` espd-input-${this.id}-directions` : '') || null;
        }
    }
    registerDelegate(delegate) {
        this.delegates.push(delegate);
    }
    updatePlaceholder(value) {
        this.placeholderText = this.messageService.evaluate(value);
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: EspdAbstractInputDirective, deps: [{ token: i0.Injector }], target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "20.3.15", type: EspdAbstractInputDirective, isStandalone: true, inputs: { label: "label", labelledby: "labelledby", labelSrOnly: "labelSrOnly", directions: "directions", disabled: "disabled", required: "required", hideErrorMessages: "hideErrorMessages", errorMessages: "errorMessages", placeholder: "placeholder" }, usesInheritance: true, usesOnChanges: true, ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: EspdAbstractInputDirective, decorators: [{
            type: Directive
        }], ctorParameters: () => [{ type: i0.Injector }], propDecorators: { label: [{
                type: Input
            }], labelledby: [{
                type: Input
            }], labelSrOnly: [{
                type: Input
            }], directions: [{
                type: Input
            }], disabled: [{
                type: Input
            }], required: [{
                type: Input
            }], hideErrorMessages: [{
                type: Input
            }], errorMessages: [{
                type: Input
            }], placeholder: [{
                type: Input
            }] } });

class EspdAbstractSizeableInputDirective extends EspdAbstractInputDirective {
    constructor() {
        super(...arguments);
        this.width = Sizes.lg;
    }
    set size(value) {
        this.width = Sizes[value] || Sizes.lg;
    }
    get size() {
        return Sizes[this.width];
    }
    ngOnInit() {
        super.ngOnInit();
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: EspdAbstractSizeableInputDirective, deps: null, target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "20.3.15", type: EspdAbstractSizeableInputDirective, isStandalone: true, inputs: { size: "size" }, usesInheritance: true, ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: EspdAbstractSizeableInputDirective, decorators: [{
            type: Directive
        }], propDecorators: { size: [{
                type: Input
            }] } });

/**
 * Generated bundle index. Do not edit.
 */

export { ControlNameDirective, ControlNameModule, EspdAbstractInputDirective, EspdAbstractSizeableInputDirective, EspdFormArray, EspdFormControl, EspdFormGroup, Sizes, clearNestedServerErrors, espdAbstractInputComponentInjectionToken, getAllFormValidationErrors, isAncestorSubmitted, isEspdControl };
//# sourceMappingURL=espd-common-forms-api.mjs.map
