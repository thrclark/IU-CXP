import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Validators, UntypedFormArray, NG_VALIDATORS } from '@angular/forms';
import { ServiceLocator } from 'espd-common/utils';
import { of, BehaviorSubject } from 'rxjs';
import { catchError, map, debounceTime, switchMap, first, distinctUntilChanged } from 'rxjs/operators';
import * as i0 from '@angular/core';
import { forwardRef, Input, Directive } from '@angular/core';

/* eslint-disable @typescript-eslint/naming-convention */
class EspdValidators {
    static { this.uniqueKeyPattern = Validators.pattern(new RegExp('^[a-z0-9\\-]+$')); }
    static { this.hexColorPattern = Validators.pattern(/^#[A-Fa-f0-9]{6}$/); }
    static equalTo(equalControl) {
        let subscribe = false;
        return (control) => {
            if (!subscribe) {
                subscribe = true;
                equalControl.valueChanges.subscribe(() => {
                    control.updateValueAndValidity();
                });
            }
            const v = control.value;
            return equalControl.value === v ? null : { equalTo: { control: equalControl, value: equalControl.value } };
        };
    }
    static minDate(minDate) {
        return (control) => {
            const date = control.value;
            if (!date || date.getTime() >= minDate.getTime()) {
                return null;
            }
            else {
                return { 'min-date': { value: control.value, expected: minDate } };
            }
        };
    }
    static unique(url, entityId) {
        const http = ServiceLocator.get(HttpClient);
        return AsyncObservableValidator.create(value => {
            const data = { value, entityId };
            if (http) {
                return http.post(url, JSON.stringify(data), {
                    headers: new HttpHeaders({
                        'Content-Type': 'application/json',
                        'X-Requested-With': 'XMLHttpRequest'
                    })
                }).pipe(catchError(() => of(true)), map((response) => !response ? { unique: true } : null));
            }
            else {
                return of(null);
            }
        });
    }
    static url() {
        function doValidate(control) {
            if (Validators.required(control)) {
                return null;
            }
            const v = control.value;
            const regEx = /^(?:(?:(?:https?|ftp):)?\/\/)(?:\S+(?::\S*)?@)?(?:(?!(?:10|127)(?:\.\d{1,3}){3})(?!(?:169\.254|192\.168)(?:\.\d{1,3}){2})(?!172\.(?:1[6-9]|2\d|3[0-1])(?:\.\d{1,3}){2})(?:[1-9]\d?|1\d\d|2[01]\d|22[0-3])(?:\.(?:1?\d{1,2}|2[0-4]\d|25[0-5])){2}(?:\.(?:[1-9]\d?|1\d\d|2[0-4]\d|25[0-4]))|(?:(?:[a-z\u00a1-\uffff0-9]-*)*[a-z\u00a1-\uffff0-9]+)(?:\.(?:[a-z\u00a1-\uffff0-9]-*)*[a-z\u00a1-\uffff0-9]+)*(?:\.(?:[a-z\u00a1-\uffff]{2,})).?)(?::\d{2,5})?(?:[/?#]\S*)?$/i;
            return regEx.test(v) ? null : { url: true };
        }
        return doValidate;
    }
    static conditional(conditionalFn, validator) {
        function doValidate(control) {
            revalidateOnChanges(control);
            if (conditionalFn(control)) {
                return validator?.(control) ?? null;
            }
            else {
                return null;
            }
        }
        return doValidate;
    }
    static arrayMaxLength(max) {
        return (control) => {
            if (!(control instanceof UntypedFormArray))
                return;
            return control.length > max ? { arrayMaxLength: true } : null;
        };
    }
}
/**
 * Async validator that debounces
 */
class AsyncObservableValidator {
    static create(callback, debounceTimeMs = 1000) {
        const valueSubject = new BehaviorSubject(null);
        const valueObservable = valueSubject.asObservable();
        return (control) => {
            valueSubject.next(control.value);
            return valueObservable.pipe(debounceTime(debounceTimeMs), switchMap(value => callback(value)), first());
        };
    }
}
const revalidateOnChanges = (control) => {
    if (control && control.parent && !control.revalidateOnChanges) {
        control.revalidateOnChanges = true;
        let root = control.parent;
        while (root.parent) {
            root = root.parent;
        }
        root.valueChanges
            .pipe(distinctUntilChanged((a, b) => equals(a, b)))
            .subscribe(() => {
            control.updateValueAndValidity();
        });
        control.updateValueAndValidity();
    }
};
const equals = (x, y) => {
    if (x === y) {
        return true;
    }
    // if both x and y are null or undefined and exactly the same
    if (!(x instanceof Object) || !(y instanceof Object)) {
        return false;
    }
    // if they are not strictly equal, they both need to be Objects
    if (x.constructor !== y.constructor) {
        return false;
    }
    // they must have the exact same prototype chain, the closest we can do is
    // test there constructor.
    for (const p in x) {
        if (!Object.prototype.hasOwnProperty.call(x, p)) {
            continue;
        }
        // other properties were tested using x.constructor === y.constructor
        if (!Object.prototype.hasOwnProperty.call(y, p)) {
            return false;
        }
        // allows to compare x[ p ] and y[ p ] when set to undefined
        if (x[p] === y[p]) {
            continue;
        }
        // if they have the same strict value or identity then they are equal
        if (typeof (x[p]) !== 'object') {
            return false;
        }
        // Numbers, Strings, Functions, Booleans must be strictly equal
        if (!equals(x[p], y[p])) {
            return false;
        }
        // Objects and Arrays must be tested recursively
    }
    for (const p in y) {
        if (Object.prototype.hasOwnProperty.call(y, p) && !Object.prototype.hasOwnProperty.call(x, p)) {
            return false;
        }
        // allows x[ p ] to be set to undefined
    }
    return true;
};

/* eslint-disable @typescript-eslint/member-ordering */
const EQUAL_TO_VALIDATOR = {
    provide: NG_VALIDATORS,
    useExisting: forwardRef(() => EqualToValidator),
    multi: true
};
class EqualToValidator {
    constructor() {
        this.validator = Validators.nullValidator;
    }
    ngOnInit() {
        if (this.equalTo) {
            this.validator = EspdValidators.equalTo(this.equalTo);
        }
    }
    validate(c) {
        return this.validator(c);
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.1", ngImport: i0, type: EqualToValidator, deps: [], target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "20.3.1", type: EqualToValidator, isStandalone: true, selector: "[espdEqualTo][formControlName],[espdEqualTo][formControl],[espdEqualTo][ngModel]", inputs: { equalTo: ["espdEqualTo", "equalTo"] }, providers: [EQUAL_TO_VALIDATOR], ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.1", ngImport: i0, type: EqualToValidator, decorators: [{
            type: Directive,
            args: [{
                    selector: '[espdEqualTo][formControlName],[espdEqualTo][formControl],[espdEqualTo][ngModel]',
                    providers: [EQUAL_TO_VALIDATOR],
                    standalone: true
                }]
        }], propDecorators: { equalTo: [{
                type: Input,
                args: ['espdEqualTo']
            }] } });

/**
 * Generated bundle index. Do not edit.
 */

export { EqualToValidator, EspdValidators };
//# sourceMappingURL=espd-common-forms-validators.mjs.map
