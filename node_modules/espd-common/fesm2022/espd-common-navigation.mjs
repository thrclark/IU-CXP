import { Location } from '@angular/common';
import * as i0 from '@angular/core';
import { inject, Injectable } from '@angular/core';
import { Router, UrlSerializer, NavigationEnd } from '@angular/router';

class NavigationService {
    constructor() {
        this.history = [];
        this.location = inject(Location);
        this.router = inject(Router);
        this.urlSerializer = inject(UrlSerializer);
        this.router.events.subscribe((event) => {
            if (event instanceof NavigationEnd) {
                this.history.push(event.urlAfterRedirects);
            }
        });
    }
    back(fallbackCommands, fallbackExtras) {
        this.history.pop();
        if (this.history.length > 0) {
            this.location.back();
        }
        else {
            if (fallbackCommands) {
                void this.router.navigate(fallbackCommands, fallbackExtras);
            }
            else {
                void this.router.navigateByUrl('/');
            }
        }
    }
    previousRoute(fallbackRoute) {
        const prevousRoute = this.history.at(-2);
        if (prevousRoute) {
            return prevousRoute;
        }
        else {
            return fallbackRoute;
        }
    }
    findPastRoute(basicRoute) {
        let queryParamRoute;
        for (let index = this.history.length - 1; index >= 0; index--) {
            const urltree = this.urlSerializer.parse(this.history[index]);
            if (basicRoute === '/' + urltree.root.children.primary?.toString()) {
                queryParamRoute = this.history[index];
                break;
            }
        }
        if (queryParamRoute) {
            return queryParamRoute;
        }
        else {
            return basicRoute;
        }
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: NavigationService, deps: [], target: i0.ɵɵFactoryTarget.Injectable }); }
    static { this.ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: NavigationService }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: NavigationService, decorators: [{
            type: Injectable
        }], ctorParameters: () => [] });

/**
 * Generated bundle index. Do not edit.
 */

export { NavigationService };
//# sourceMappingURL=espd-common-navigation.mjs.map
