import * as i0 from '@angular/core';
import { Injectable } from '@angular/core';
import * as i1 from '@angular/router';
import { NavigationEnd } from '@angular/router';
import * as i2 from '@angular/common';

class NavigationService {
    constructor(router, location, urlSerializer) {
        this.router = router;
        this.location = location;
        this.urlSerializer = urlSerializer;
        this.history = [];
        this.router.events.subscribe((event) => {
            if (event instanceof NavigationEnd) {
                this.history.push(event.urlAfterRedirects);
            }
        });
    }
    back(fallbackCommands, fallbackExtras) {
        this.history.pop();
        if (this.history.length > 0) {
            this.location.back();
        }
        else {
            if (fallbackCommands) {
                this.router.navigate(fallbackCommands, fallbackExtras);
            }
            else {
                this.router.navigateByUrl('/');
            }
        }
    }
    previousRoute(fallbackRoute) {
        const prevousRoute = this.history.at(-2);
        if (prevousRoute) {
            return prevousRoute;
        }
        else {
            return fallbackRoute;
        }
    }
    findPastRoute(basicRoute) {
        let queryParamRoute;
        for (let index = this.history.length - 1; index >= 0; index--) {
            const urltree = this.urlSerializer.parse(this.history[index]);
            if (basicRoute === '/' + urltree.root.children.primary?.toString()) {
                queryParamRoute = this.history[index];
                break;
            }
        }
        if (queryParamRoute) {
            return queryParamRoute;
        }
        else {
            return basicRoute;
        }
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.1", ngImport: i0, type: NavigationService, deps: [{ token: i1.Router }, { token: i2.Location }, { token: i1.UrlSerializer }], target: i0.ɵɵFactoryTarget.Injectable }); }
    static { this.ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "20.3.1", ngImport: i0, type: NavigationService }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.1", ngImport: i0, type: NavigationService, decorators: [{
            type: Injectable
        }], ctorParameters: () => [{ type: i1.Router }, { type: i2.Location }, { type: i1.UrlSerializer }] });

/**
 * Generated bundle index. Do not edit.
 */

export { NavigationService };
//# sourceMappingURL=espd-common-navigation.mjs.map
