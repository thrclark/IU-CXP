import * as i0 from '@angular/core';
import { inject, Injectable } from '@angular/core';
import { BsModalService } from 'ngx-bootstrap/modal';

class PlaceholderService {
    constructor() {
        this.bsModalService = inject(BsModalService);
        this.bsModalService.onShow.subscribe(() => {
            this.saveFocusLocation();
        });
        this.bsModalService.onHidden.subscribe(() => {
            this.resume();
        });
    }
    saveFocusLocation(dropdownButton, dropdownDirective) {
        this.lastFocusedElement = document.activeElement;
        if (dropdownButton) {
            this.dropdownButton = dropdownButton;
            this.dropdownDirective = dropdownDirective;
            this.index = dropdownDirective?.indexOf(this.lastFocusedElement);
        }
    }
    deleteFocusLocation() {
        this.dropdownButton = undefined;
        this.lastFocusedElement = undefined;
    }
    resume() {
        const dropdownButton = this.dropdownButton;
        const dropdownDirective = this.dropdownDirective;
        const index = this.index;
        this.dropdownButton = undefined;
        this.dropdownDirective = undefined;
        this.index = undefined;
        if (dropdownButton && document.body.contains(dropdownButton)) {
            dropdownButton.click();
        }
        setTimeout(() => {
            if (index !== undefined && index !== -1) {
                dropdownDirective?.focusOnIndex(index);
            }
            else if (this.lastFocusedElement && document.body.contains(this.lastFocusedElement)) {
                this.lastFocusedElement.focus();
            }
            this.lastFocusedElement = undefined;
        });
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: PlaceholderService, deps: [], target: i0.ɵɵFactoryTarget.Injectable }); }
    static { this.ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: PlaceholderService }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.15", ngImport: i0, type: PlaceholderService, decorators: [{
            type: Injectable
        }], ctorParameters: () => [] });

/**
 * Generated bundle index. Do not edit.
 */

export { PlaceholderService };
//# sourceMappingURL=espd-common-placeholder.mjs.map
