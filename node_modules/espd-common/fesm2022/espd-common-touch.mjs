import * as i0 from '@angular/core';
import { EventEmitter, HostListener, Output, Input, Directive } from '@angular/core';

class SwipeDirective {
    constructor() {
        this.orientation = 'horizontal';
        this.next = new EventEmitter();
        this.previous = new EventEmitter();
        this.startCoord = [0, 0];
        this.startTime = 0;
    }
    onTouchStart($event) {
        this.startCoord = [$event.changedTouches[0].clientX, $event.changedTouches[0].clientY];
        this.startTime = new Date().getTime();
    }
    onTouchEnd($event) {
        const coord = [$event.changedTouches[0].clientX, $event.changedTouches[0].clientY];
        const vector = [coord[0] - this.startCoord[0], coord[1] - this.startCoord[1]];
        const duration = new Date().getTime() - this.startTime;
        const hypotenuse = Math.sqrt(vector[0] * vector[0] + vector[1] * vector[1]);
        if (duration < 1000 // Quick enough
            && Math.abs(hypotenuse) >= 30 // Far enough
            && this.isCorrectOrientation(vector)) {
            if (vector[this.orientation === 'horizontal' ? 0 : 1] < 0) {
                this.next.emit();
            }
            else {
                this.previous.emit();
            }
        }
    }
    isCorrectOrientation(vector) {
        const primaryComponent = this.orientation === 'horizontal' ? 0 : 1;
        const secondaryComponent = 1 - primaryComponent;
        return Math.abs(vector[primaryComponent]) > Math.abs(vector[secondaryComponent] * 3); // primary component is greater than 3x secondary component
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "20.3.1", ngImport: i0, type: SwipeDirective, deps: [], target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "20.3.1", type: SwipeDirective, isStandalone: true, selector: "[espdSwipe]", inputs: { orientation: "orientation" }, outputs: { next: "next", previous: "previous" }, host: { listeners: { "touchstart": "onTouchStart($event)", "touchend": "onTouchEnd($event)" } }, ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "20.3.1", ngImport: i0, type: SwipeDirective, decorators: [{
            type: Directive,
            args: [{
                    selector: '[espdSwipe]',
                    standalone: true
                }]
        }], propDecorators: { orientation: [{
                type: Input
            }], next: [{
                type: Output
            }], previous: [{
                type: Output
            }], onTouchStart: [{
                type: HostListener,
                args: ['touchstart', ['$event']]
            }], onTouchEnd: [{
                type: HostListener,
                args: ['touchend', ['$event']]
            }] } });

/**
 * Generated bundle index. Do not edit.
 */

export { SwipeDirective };
//# sourceMappingURL=espd-common-touch.mjs.map
