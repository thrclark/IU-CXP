import { HttpClient } from '@angular/common/http';
import { ApplicationConfig } from 'espd-common/config';
import { Observable } from 'rxjs';
import * as i0 from '@angular/core';

declare enum SettingType {
    SESSION_DURATION_SECONDS = "common.SESSION_DURATION_SECONDS",
    CUSTOM_FOOTER = "common.CUSTOM_FOOTER",
    CUSTOM_HEADER = "common.CUSTOM_HEADER",
    LOGO_URL = "common.LOGO_URL",
    LOGO_URL_MOBILE = "common.LOGO_URL_MOBILE",
    LOGO_DISPLAY = "common.LOGO_DISPLAY"
}
declare abstract class SettingService {
    private http;
    private config;
    protected settings: {
        [key: string]: any;
    };
    private loadedModules;
    private dataUpdateSubject;
    readonly onDataUpdate: Observable<string>;
    constructor(http: HttpClient, config: ApplicationConfig);
    init(module: string): any;
    get(key: string): any;
    waitFor(...modules: string[]): Observable<void>;
    /** A method to allow parsing or modifying of the setting data after it is retrieved, but before it is released to subscribers. */
    abstract initializeSettings(module: string): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<SettingService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<SettingService>;
}

declare class SettingServiceImpl extends SettingService {
    initializeSettings(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<SettingServiceImpl, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<SettingServiceImpl>;
}

export { SettingService, SettingServiceImpl, SettingType };
